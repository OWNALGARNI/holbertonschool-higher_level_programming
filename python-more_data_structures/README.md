0. Squared simple
Write a function that returns a new matrix where each value is squared, without modifying the original matrix.

1. Search and replace
Replace occurrences of a given element in a list and return a new list.

2. Unique addition
Compute the sum of all unique integers in a list.

3. Present in both
Return a set of common elements between two sets.

4. Only differents
Return a set of elements found in only one of the two sets.

5. Number of keys
Return the number of keys in a dictionary.

6. Print sorted dictionary
Print a dictionary sorted by key.

7. Update dictionary
Replace or add a key-value pair in a dictionary.

8. Simple delete by key
Delete a key from a dictionary if it exists.

9. Multiply by 2
Return a new dictionary with all integer values multiplied by 2.

10. Best score
Return the key with the highest value in a dictionary.

11. Multiply by using map
Use `map` and `lambda` to multiply all integers in a list.

12. Roman to Integer
Convert a Roman numeral string to an integer.
