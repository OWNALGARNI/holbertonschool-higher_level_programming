Files and Tasks

0. Simple rectangle
**File:** `0-rectangle.py`  
Defines an empty `Rectangle` class.

---

1. Real definition of a rectangle
**File:** `1-rectangle.py`  
Defines a rectangle with private instance attributes `width` and `height`
and validates their type and value.

---
2. Area and Perimeter
**File:** `2-rectangle.py`  
Adds methods to calculate the area and perimeter of the rectangle.

---

3. String representation
**File:** `3-rectangle.py`  
Implements the `__str__` method to print the rectangle using the `#` character.

---

4. Eval is magic
**File:** `4-rectangle.py`  
Implements the `__repr__` method to return a string that can recreate the object
using `eval()`.

---
# 5. Detect instance deletion
**File:** `5-rectangle.py`  
Implements the `__del__` method to print a message when a rectangle is deleted.

---

6. How many instances
**File:** `6-rectangle.py`  
Adds a class attribute to track the number of Rectangle instances.

---

7. Change representation
**File:** `7-rectangle.py`  
Allows changing the symbol used to print the rectangle.

---

8. Compare rectangles
**File:** `8-rectangle.py`  
Adds a static method to compare two rectangles based on their area.

---

### 9. A square is a rectangle
**File:** `9-rectangle.py`  
Adds a class method that returns a new Rectangle instance representing a square.

